[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sigard-ml"
version = "0.1.0"
description = "Pipelines reproducibles de datos y ML para SIGARD"
requires-python = ">=3.11"
dependencies = [
  "geopandas>=1.0",
  "pandas>=2.2",
  "pyarrow>=15",
  "scikit-learn>=1.5,<2",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
sigard-build-territorial = "sigard_ml.ingestion.territorial:main"
sigard-build-temporal = "sigard_ml.ingestion.temporal:main"
sigard-build-synthetic = "sigard_ml.simulation.pipeline:main"
sigard-build-modeling-panel = "sigard_ml.features.pipeline:main"
sigard-evaluate-baseline = "sigard_ml.evaluation.pipeline:main"
sigard-train-random-forest = "sigard_ml.evaluation.random_forest_pipeline:main"
sigard-adjust-random-forest = "sigard_ml.evaluation.random_forest_variants_pipeline:main"
sigard-export-mvp = "sigard_ml.presentation.mvp_export:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
